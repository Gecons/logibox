# logibox
A console-based virtual sandbox environment that does logic computations. This program allows the user to experiment with logic to build standalone complex computational logic architectures.
